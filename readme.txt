Make sure python is installed
Make sure chrome is installed


In command line:

cd into folder
pip install pyyaml
pip install selenium
Change data in addyourinformation.yaml file
Make sure you have a resume uploaded to your linkedin (you can run the bot and see if it gets stuck at the resume selection part)
run python bot.py